<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mcs_lec_project.gamicality">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gamicality">
        <activity android:name=".ReplyActivity"
            android:parentActivityName=".PostDetailActivity" />
        <activity android:name=".AddPostActivity"
            android:parentActivityName=".PostDetailActivity" />
        <activity android:name=".RegisterActivity"
            android:parentActivityName=".LoginActivity"/>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BookmarkActivity"
            android:label="Bookmark"/>
        <activity android:name=".NotificationActivity"
            android:label="Notification"/>
        <activity android:name=".PostDetailActivity"
            android:parentActivityName=".PostIndexActivity"/>
        <activity android:name=".GameListActivity">
        </activity>
        <activity android:name=".PostIndexActivity"
            android:label="Gamicality"
            android:parentActivityName=".GameListActivity"/>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EditProfileActivity"
            android:parentActivityName=".ProfileActivity"/>
    </application>
</manifest>